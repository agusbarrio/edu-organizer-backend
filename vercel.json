{
  "builds": [
    {
      "src": "index.js",
      "use": "@vercel/node",
      "buildCommand": "npm run vercel-build && npm run migrate"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "index.js"
    }
  ]
}
